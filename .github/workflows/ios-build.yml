name: iOS Build

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    # Project targets iOS 26.2 / Swift 6.0 — macos-15's Xcode 16.4 (iOS 18.5
    # SDK) fails on upstream code (glassEffect etc.). Need Xcode 26.
    runs-on: macos-26
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install deps toolchain
        run: |
          # lld is a separate Homebrew formula since LLVM was split (2026).
          # brew meson instead of pip3: runner Python is PEP-668 managed.
          brew install meson ninja llvm lld libarchive pkg-config

      - name: Prepare xcconfig
        run: |
          cd src/ios
          cp Configs/ProviderCustomization.xcconfig.example Configs/ProviderCustomization.xcconfig

      # M1 note: the iSH sandbox rootfs (alpine-rootfs.zip, RootfsPatch.bundle)
      # is generated by deps/prepare_alpine_rootfs.sh and is not committed.
      # The remote-agent feature (M1) does not use the sandbox, so stubs pass
      # the Resources build phase; real assets land before local-agent use.
      - name: Stub sandbox resources (M1)
        run: |
          mkdir -p deps/resources
          touch deps/resources/alpine-rootfs.zip
          mkdir -p deps/resources/RootfsPatch.bundle

      - name: Build native deps (lame → ffmpeg → ish)
        run: |
          # VDSO needs lld; meson picks `clang` from PATH, so brew LLVM must lead.
          export PATH="/opt/homebrew/opt/llvm/bin:/opt/homebrew/opt/lld/bin:$PATH"
          ./deps/build_lame.sh
          ./deps/build_ffmpeg.sh
          ./deps/build_ish.sh

      - name: Archive & sign (free personal team)
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_ID_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
        run: |
          cd src/ios
          xcodebuild archive \
            -project Minis.xcodeproj \
            -scheme Minis \
            -configuration Debug \
            -destination 'generic/platform=iOS' \
            -archivePath build/Minis.xcarchive \
            -allowProvisioningUpdates \
            -allowProvisioningDeviceRegistration
          cat > build/export-options.plist <<'PLIST'
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>method</key>
  <string>development</string>
  <key>signingStyle</key>
  <string>automatic</string>
</dict>
</plist>
PLIST
          xcodebuild -exportArchive \
            -archivePath build/Minis.xcarchive \
            -exportPath build/export \
            -exportOptionsPlist build/export-options.plist

      - name: Upload .ipa
        uses: actions/upload-artifact@v4
        with:
          name: Minis-signed-ipa
          path: src/ios/build/export/*.ipa
          if-no-files-found: error
